name: Release
on:
  push:
    tags: ["v*"]

# Stub: wire real publishing (npm / GitHub Release) when the CLI ships (Step 5).
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version-file: .nvmrc }
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run check
      - run: bun run build
      - run: echo "TODO: publish artifact for ${GITHUB_REF_NAME}"
